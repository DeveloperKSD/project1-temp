Architecture: 
main.py
agent/agent.py 
     tools.py
     memory.py
     policies.py
data/memory.json
    tickets.json

checkmodels.py not needed in final execution, only used to check the current running ai model/api

ALSO REPLACE tools.py with your api key and model name
if not sure use which one just run checkmodel.py
also download all the libraries from requirements.txt
TO RUN- go to cmd with directory to the project and then run main.py
STEPS:
1. make a folder named project and add all the files there, following the given architecture
2. go to folder and open cmd
3. pip install -r requirements.txt
4. put your api key and model in tools.py
5. if you want to check the model name, just run the checkmodel.py
6. after all is ddone, run main.py and YIPPEEE
